// functions

const { initializeApp } = require("firebase");

/**
 * - nomaclature
    - name, body, posession(parmeterBracket)
    - declare
    - execute
    - non-parm functin
    - paramed function
    - callback
    - anonymous function
    - function inside a variable
    - IIFI ( immediate invoke functions )
*/

    // anonymous func
     const testFunc = ()=>{

     }

    // bare functions
      function testFunc2(){

      }


// DECLARATION
 int x ; // declare
 x  = 3; //initializeApp
 print(x); //execute

 const x = 3;
 = ( assignment)
 == ( shallow compare)
 === ( deep compare)
 ! ( not /inverser)
